import './globals.css';
export const metadata = { title: 'RDC Customer and Vendor Reconciliation' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
